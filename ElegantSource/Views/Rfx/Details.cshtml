@model ElegantSource.Models.Rfx.Rfx

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>Rfx</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">AllowComment</div>
    <div class="display-field">@Model.AllowComment</div>

    <div class="display-label">ResponseDeadline</div>
    <div class="display-field">@String.Format("{0:g}", Model.ResponseDeadline)</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>


